# Apache on :8282 — document root is already src/public (no /dbapi/src prefix).
BASE_URL=http://127.0.0.1:8282/
MGMT_KEY=myverysecuresecret

# dbapi-mysql-1 from docker compose (host port 3308 → container 3306)
DB_HOST=127.0.0.1
DB_PORT=3308
DB_USER=user
DB_PASS=password
DB_NAME=myapp
